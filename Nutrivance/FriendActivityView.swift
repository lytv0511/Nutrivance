import SwiftUI

struct FriendActivityView: View {
    var body: some View {
        ComingSoonView(
            feature: "FriendActivity",
            description: "Experience the future of fitness tracking with FriendActivity"
        )
    }
}
