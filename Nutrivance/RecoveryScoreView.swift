import SwiftUI

struct RecoveryScoreView: View {
    var body: some View {
        NavigationStack{
            ComingSoonView(
                feature: "RecoveryScore",
                description: "Experience the future of fitness tracking with RecoveryScore"
            )
        }
    }
}
