import SwiftUI

struct TodaysPlanView: View {
    var body: some View {
        ComingSoonView(
            feature: "TodaysPlan",
            description: "Experience the future of fitness tracking with TodaysPlan"
        )
    }
}
