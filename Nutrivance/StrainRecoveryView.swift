import SwiftUI

struct StrainRecoveryView: View {
    var body: some View {
        ComingSoonView(
            feature: "StrainRecovery",
            description: "Experience the future of fitness tracking with StrainRecovery"
        )
    }
}
