import SwiftUI

struct PostWorkoutWindowView: View {
    var body: some View {
        ComingSoonView(
            feature: "PostWorkoutWindow",
            description: "Experience the future of fitness tracking with PostWorkoutWindow"
        )
    }
}
