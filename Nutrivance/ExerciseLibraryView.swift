import SwiftUI

struct ExerciseLibraryView: View {
    var body: some View {
        ComingSoonView(
            feature: "ExerciseLibrary",
            description: "Experience the future of fitness tracking with ExerciseLibrary"
        )
    }
}
