import SwiftUI

struct AchievementsView: View {
    var body: some View {
        ComingSoonView(
            feature: "Achievements",
            description: "Experience the future of fitness tracking with Achievements"
        )
    }
}
