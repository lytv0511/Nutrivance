import SwiftUI

struct ProgramBuilderView: View {
    var body: some View {
        ComingSoonView(
            feature: "ProgramBuilder",
            description: "Experience the future of fitness tracking with ProgramBuilder"
        )
    }
}
