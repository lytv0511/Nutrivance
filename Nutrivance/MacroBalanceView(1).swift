import SwiftUI

struct MacroBalanceView: View {
    var body: some View {
        ComingSoonView(
            feature: "MacroBalance",
            description: "Experience the future of fitness tracking with MacroBalance"
        )
    }
}
