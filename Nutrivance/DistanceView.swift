import SwiftUI

struct DistanceView: View {
    var body: some View {
        ComingSoonView(
            feature: "Distance",
            description: "Experience the future of fitness tracking with Distance"
        )
    }
}
