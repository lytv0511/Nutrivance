import SwiftUI

struct PreWorkoutTimingView: View {
    var body: some View {
        ComingSoonView(
            feature: "PreWorkoutTiming",
            description: "Experience the future of fitness tracking with PreWorkoutTiming"
        )
    }
}
