import SwiftUI

struct WorkoutGeneratorView: View {
    var body: some View {
        ComingSoonView(
            feature: "WorkoutGenerator",
            description: "Experience the future of fitness tracking with WorkoutGenerator"
        )
    }
}
