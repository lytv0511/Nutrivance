import SwiftUI

struct StepCountView: View {
    var body: some View {
        ComingSoonView(
            feature: "StepCount",
            description: "Experience the future of fitness tracking with StepCount"
        )
    }
}
