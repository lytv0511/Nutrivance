import SwiftUI

struct LeaderboardsView: View {
    var body: some View {
        ComingSoonView(
            feature: "Leaderboards",
            description: "Experience the future of fitness tracking with Leaderboards"
        )
    }
}
