import SwiftUI

struct PersonalRecordsView: View {
    var body: some View {
        ComingSoonView(
            feature: "PersonalRecords",
            description: "Experience the future of fitness tracking with PersonalRecords"
        )
    }
}
