import SwiftUI

struct ShareWorkoutsView: View {
    var body: some View {
        ComingSoonView(
            feature: "ShareWorkouts",
            description: "Experience the future of fitness tracking with ShareWorkouts"
        )
    }
}
