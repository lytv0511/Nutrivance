import SwiftUI

struct ReadinessCheckView: View {
    var body: some View {
        ComingSoonView(
            feature: "ReadinessCheck",
            description: "Experience the future of fitness tracking with ReadinessCheck"
        )
    }
}
