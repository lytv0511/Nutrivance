import SwiftUI

struct ActivityRingsView: View {
    var body: some View {
        ComingSoonView(
            feature: "ActivityRings",
            description: "Experience the future of fitness tracking with ActivityRings"
        )
    }
}
